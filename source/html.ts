import { AppProps } from './app';

export declare const getHtml: (
  appHtml?: string,
  headTags?: string,
  initialState?: AppProps,
) => string;

export declare const routes: Array<string | RegExp>;
